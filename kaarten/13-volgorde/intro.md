# Volgorde

Logische operators hebben een vaste volgorde, net als rekenen.

- `!` (niet) gaat eerst.
- `&&` (en) gaat daarna.
- `||` (of) gaat als laatste.

Zo bindt `&&` sterker dan `||`, net zoals keer sterker bindt dan plus. Met haakjes bepaal je de volgorde zelf.
